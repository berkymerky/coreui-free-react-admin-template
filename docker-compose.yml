version: "3.8"

services:
  coreui-react:
    container_name: coreui-react
    build: .
    ports:
      - "5173:5173"
    volumes:
      - /data/snapology-dashboard-dev:/app
      - coreui_node_modules:/app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    restart: unless-stopped
